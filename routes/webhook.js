// import express from 'express';
// import { facebookWebhookHandler } from '../controllers/webhook.js';

// const router = express.Router();

// router.get('/', (req, res) => facebookWebhookHandler.handleWebhook(req, res));
// router.post('/', (req, res) => facebookWebhookHandler.handleWebhook(req, res));

// export default router;